# !!! значения атрибутов процессов, если их несколько на атрибут, разделяются подстрокой "; " !!!
commands: # 
  - type: # тип команды (stop, ...) 
    process_ids: # id процесса(ов)
    hosts: # хосты
    queues: # очереди
    task_ids: # id заданий
