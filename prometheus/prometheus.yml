global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "bloodhound_gang"
    static_configs:
      - targets: ["bloodhound_gang:8000"]  # имя сервиса и порт из HTTP_METRICS_PORT (должен совпадать)