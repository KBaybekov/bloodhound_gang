| Название эксперимента                                                                   | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                  | Действия                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Пройдено | Затрагиваемый метод                       |
| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------------------------------------------- |
| Создание процессов при отсутствии предыдущих результатов | Для образца с исходными данными, где нет результата типа`basecalling_basic`, `process_factory` должна создать один объект `Process` на каждый уникальный batch_id с корректными `process_id`, `weight`, `pipeline_vars`.                                                                                                          | 1. Создать объект Sample с одним или несколькими batch без предшествующих результатов basecalling_basic. 2. Вызвать task.create_sample_processes(sample). 3. Проверить, что получен словарь с одним Process на каждый batch, process_id формируется корректно, weight = source.size_GB, pipeline_vars содержит input_dir, outdir, sample.                                                                                                                                                      | Да             | `process_factory`, `Process.from_sources`               |
| Пропуск создания при наличии результата`basecalling_basic`             | Если для конкретного batch уже существует результат с типом`basecalling_basic`, то процесс для него не должен создаваться.                                                                                                                                                                                                                                     | 1. Добавить в sample.data.result объект ResultBasecallingBasic с tags, совпадающим с batch_id. 2. Вызвать task.create_sample_processes(sample). 3. Убедиться, что для этого batch процесс не создан.                                                                                                                                                                                                                                                                                                                                                             | Нет           | `process_factory`                                         |
| Корректное заполнение`pipeline_vars`                                                  | Проверить, что в созданном процессе поле`pipeline_vars` содержит ожидаемые ключи (`input_dir`, `outdir`, `sample`) и значения путей экранированы / являются posix-строками.                                                                                                                                                             | 1. Создать процесс через process_factory. 2. Проверить значения`proc.pipeline_vars`: `input_dir` == source.path.as_posix(), `outdir` == proc.res_d.as_posix(), `sample` == sample.sample_id.                                                                                                                                                                                                                                                                                                                                                                                               | Нет           | `process_factory`                                         |
| `result_factory`: успешный сбор всех ожидаемых файлов                      | Смоделировать директорию результатов, содержащую необходимые файлы (`.ubam`, `basecalling_source_files_metadata.tsv`, `pod5/`, `multiqc_report.html`, `used_model.txt`, `sequali.json`/`.html`). `result_factory` должна вернуть `is_processing_ok=True` и заполненный `ResultBasecallingBasic` с непустыми `basecall_data`. | 1. Создать временную структуру каталогов с перечисленными файлами. 2. Установить`process.res_d` на эту директорию. 3. Вызвать `result_factory(process)`. 4. Проверить, что `is_processing_ok` истинно, результат содержит `source_files_metadata`, `generated_pod5s_d`, `multiqc_f`, и список `basecall_data` содержит объекты `UbamONT` с корректными атрибутами (`batch`, `pore`, `model`, `molecule`, пути к qc-файлам). | Нет           | `result_factory`, `define_used_ubam_model`, `UbamONT` |
| `result_factory`: отсутствие ubam-файлов                                                  | Если в`res_d` нет `.ubam` файлов, `result_factory` должна вернуть `is_processing_ok=False` (или успех с пустым списком). Уточнить ожидаемое поведение: должен ли отсутствие ubam считаться ошибкой обработки?                                                                                                       | 1. Создать временную директорию без ubam. 2. Вызвать`result_factory`. 3. Проверить, что `is_processing_ok` соответствует спецификации (вероятно, False), в логах сообщение об ошибке.                                                                                                                                                                                                                                                                                                                                      | Нет           | `result_factory`                                          |
| `result_factory`: отсутствие важных атрибутов (source_files_metadata и т.д.)  | Проверить, что при отсутствии файлов, заданных в`MAIN_ATTRIBUTES_FILLING`, функция возвращает ошибку, если они входят в `CRITICAL_MAIN_ATTRIBUTES`. (В текущей версии `CRITICAL_MAIN_ATTRIBUTES` пуст, поэтому тест может быть информационным).                                                            | 1. Удалить один из ожидаемых файлов (например,`multiqc_report.html`). 2. Проверить, что ошибка не возникает (т.к. критичность не задана) или при необходимости добавить файл в критические.                                                                                                                                                                                                                                                                                                         | Нет           | `result_factory`                                          |
| `result_factory`: битый exitcode или ошибка парсинга модели                   | Смоделировать ситуацию, когда файл`used_model.txt` отсутствует или пуст – поле `model` должно остаться `'UNDEFINED'`.                                                                                                                                                                                                                                                    | 1. Не создавать`used_model.txt`. 2. Вызвать `result_factory`. 3. Проверить, что у `UbamONT` model == 'UNDEFINED', ошибка не возникла.                                                                                                                                                                                                                                                                                                                                                                                                                                             | Нет           | `define_used_ubam_model`                                  |
| `ResultBasecallingBasic` валидация Pydantic                                                      | Проверить, что модель`ResultBasecallingBasic` корректно загружается/сохраняется, не вызывает ошибок при присвоении списка `basecall_data` с объектами `UbamONT`/`FastqONT`, и поле `type` всегда равно `'basecalling_basic'`.                                                                                           | 1. Создать экземпляр через`from_process` с мок-процессом. 2. Убедиться, что `type` установлен верно. 3. Попытаться создать объект с некорректными типами элементов `basecall_data` – должна быть ошибка валидации.                                                                                                                                                                                                                                                                    | Нет           | `ResultBasecallingBasic`                                  |
| Интеграционный тест: полный цикл с mock-файлами                         | Запустить задачу`basecalling_basic` на тестовом образце, где папка результатов заранее подготовлена с минимальным набором файлов, и проверить, что Watchdog корректно завершает процесс, вызывает `result_factory`, обновляет `sample.data.result` и сохраняет в БД.     | 1. Очистить БД. 2. Создать структуру`res_d` с необходимыми файлами. 3. Запустить контейнер, дождаться цикла обработки. 4. Проверить, что статус процесса `completed`, в образце появился результат с типом `basecalling_basic`, поля заполнены.                                                                                                                                                                                                                     | Нет           | `processing_wd` цепочка + `result_factory`       |
