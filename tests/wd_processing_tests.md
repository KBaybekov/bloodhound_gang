
| Название эксперимента | Описание | Действия | Пройдено | Затрагиваемый метод |
|----------------------|----------|----------|----------|----------------------|
| Загрузка конфигураций при первом цикле | Убедиться, что `tasks.yaml`, `hosts.yaml`, `queues.yaml` корректно загружаются, создаются объекты `Task`, `Host`, `Queue`. В логах отображается количество загруженных задач, хостов, очередей. | 1. Разместить корректные YAML в `conf/`. 2. Запустить приложение. 3. Проверить логи: сообщения о загрузке N tasks, N hosts, N queues. |Да| `_load_configs`, `_load_tasks`, `_load_hosts`, `_load_queues` |
| Обновление конфигураций при изменении YAML | Изменить `tasks.yaml` (добавить/удалить задачу) и проверить, что в следующем цикле `WatchdogProcessing` видит изменение `mtime`, перезагружает задачи и обновляет внутренний словарь `self.tasks`. Старые задачи удаляются, новые добавляются. | 1. Дождаться цикла с исходными задачами. 2. Изменить `tasks.yaml` (добавить или удалить задачу). 3. Дождаться следующего цикла. 4. Убедиться, что в логах новое количество задач, старых нет. | | `load_cfg_yaml_if_it_changed`, `_load_tasks` |
| Поиск подходящих образцов для задачи (с корректным `db_query`) | Создать задачу с `db_query` `{"group": "DNA"}`, убедиться, что в `_get_task_ready_samples` находятся только образцы с группой DNA. Проверить, что образцы, не соответствующие запросу, не попадают в `task_ready_samples`. | 1. Создать в БД несколько образцов с разными группами (DNA, RNA). 2. В `tasks.yaml` задать `db_query: {"group": "DNA"}`. 3. Запустить цикл. 4. Проверить, что только DNA-образцы попали в `task_ready_samples`. | | `_get_task_ready_samples`, `dao.find` |
| Исключение уже загруженных образцов при повторном поиске | После первого цикла некоторые образцы уже загружены в `self.samples`. При следующем вызове `_get_task_ready_samples` они должны исключаться из запроса к БД (через `$nin`). Убедиться, что не происходит повторной загрузки и обновления. | 1. Первый цикл загружает образцы. 2. Второй цикл: проверить логи – нет повторной загрузки тех же образцов. | | `_get_task_ready_samples` |
| Создание процессов для готовых образцов | При наличии задачи и подходящего образца вызывается `task.create_sample_processes(sample)`. Проверить, что создаются объекты `Process` с корректными `process_id`, `sample_id`, `task_id`, `tags`, `queue`, `pipeline`, `env` и т.д. Процессы добавляются в `self.processes`, а в образце обновляется `processes.created`. | 1. Настроить задачу, подходящий образец и минимальную фабрику процессов. 2. Запустить цикл. 3. Проверить БД и логи: создан процесс с ожидаемыми полями, образец обновлён. | | `_create_processes`, `task.create_sample_processes` |
| Исключение дублирующихся процессов | Если процесс с таким же `process_id` уже существует в БД (определяется по `process_id`), он не должен создаваться повторно. Проверить, что `_create_processes` сверяется с `db_stored_processes` и пропускает уже существующие. | 1. Создать процесс в БД вручную или дождаться автоматического создания. 2. Запустить следующий цикл. 3. Убедиться, что дубликат не появился. | | `_create_processes` |
| Загрузка незавершённых процессов из БД при старте | При запуске вотчдога процессы со статусами из `PROCESS_STATUSES_UNFINISHED` должны загружаться в `self.processes`. Проверить, что они корректно восстанавливаются из БД, включая специфичные поля (`work_d`, `res_d` и т.д.). | 1. Перед запуском приложения добавить в БД процесс со статусом `created`. 2. Запустить приложение. 3. Проверить, что процесс загружен в `self.processes` с корректными полями. | | `load_processes_from_db`, `from_db` |
| Планирование процессов в очереди | При наличии незавершённых процессов они должны быть сгруппированы по очередям и распланированы (`group_queue_processes`). Проверить, что процессы переходят из `created` в `scheduled`, получают `queue_number` и попадают в `processes_planned` соответствующей очереди. | 1. Настроить очереди, задачи, образцы. 2. Запустить цикл. 3. Проверить, что процессы сменили статус на `scheduled` и получили `queue_number`. | | `_form_queues`, `Queue.group_queue_processes`, `Queue.refill_planned_processes` |
| Запуск процессов с учётом ресурсов хостов | При наличии свободных ресурсов на хосте и доступных процессов в очереди, `_start_new_processes` должна выбрать хост с наименьшей занятостью, зарезервировать ресурсы, сформировать команду и запустить процесс. Проверить, что в логах появляется сообщение о запуске, процесс переходит в статус `running`, а в `running_processes` добавляется запись. | 1. Настроить хост с достаточными ресурсами, очередь, задачу. 2. Запустить цикл. 3. Проверить логи и БД: процесс стартовал, ресурсы хоста заняты. | | `_start_new_processes`, `define_process_host`, `Host.is_able_to_run_process`, `process.run` |
| Ограничение параллельных запусков (concurrency) | При `concurrency = 2` и уже двух запущенных процессах новый процесс не должен запускаться, даже если есть свободные хосты. Проверить, что `prepare_processes_for_start` возвращает пустой список, когда нет вакансий. | 1. Установить `concurrency=2` в очереди. 2. Запустить два процесса (статус `running`). 3. Добавить третий процесс в очередь. 4. Убедиться, что третий не запускается, пока один из первых двух не завершится. | | `Queue.prepare_processes_for_start`, `_start_new_processes` |
| Обнаружение завершённого процесса и сбор результатов | После завершения удалённой команды (появление exitcode-файла) `check_running` должен определить код возврата, собрать логи и вызвать `result_factory`. Проверить, что статус процесса меняется на `completed` (или `failed`), результат сохраняется в `sample.data.result`, а процесс удаляется из `running_processes`. | 1. Запустить тестовый процесс, который быстро завершается с exitcode=0. 2. Дождаться цикла `processing_wd`. 3. Проверить: статус `completed`, результат в БД у образца, процесс убран из `running`. | | `_actualize_info_running_processes`, `process.check_running`, `process.result_factory_func` |
| Обработка ошибки запуска (отсутствие pidfile) | Симулировать ошибку SSH-запуска, при которой pidfile не создаётся. Процесс должен перейти в статус `failed[bad_pidfile]`, время завершения зафиксироваться, ресурсы хоста освободиться. | 1. Настроить задачу с неверным хостом или командой, чтобы pidfile не появился. 2. Запустить цикл. 3. Проверить: статус `failed[bad_pidfile]`, ресурсы хоста свободны. | | `run_ssh_shell_detached`, `_actualize_info_running_processes` |
| Остановка процессов по пользовательской команде | Создать `user_commands.yaml` с командой `stop` для конкретного `process_id`. В следующем цикле `check_for_user_commands` должна распознать команду, найти процесс и вызвать `stop_one_process` с последующей отправкой SIGTERM/SIGKILL. Проверить, что процесс переходит в статус `cancelled[by_user]`, ресурсы освобождаются. | 1. Запустить процесс. 2. Создать/изменить `user_commands.yaml` с командой `stop` для этого процесса. 3. Дождаться цикла. 4. Проверить: статус `cancelled[by_user]`, ресурсы хоста освобождены. | | `check_for_user_commands`, `stop_processes`, `stop_one_process`, `process.terminate` |
| Перезапуск процесса после системного прерывания | Если процесс был остановлен по причине `system_interrupt` (например, перезапуск контейнера), его статус должен быть `cancelled[system_interrupt]`. При повторном планировании такой процесс должен быть перезапущен (вызов `process.run` со специальной логикой). Проверить, что старый exitcode-файл удаляется, процесс снова запускается. | 1. Создать процесс в статусе `cancelled[system_interrupt]` в БД. 2. Запустить цикл. 3. Проверить, что процесс был взят в обработку и перезапущен (статус `running`). | | `process.run`, `_load_processes`, `_form_queues` |
| Сохранение изменений в БД | Убедиться, что после каждого цикла все изменённые объекты (`Sample`, `Process`) сохраняются через `upsert_many`, а неизменённые не обновляются. Проверить, что после сохранения сбрасываются флаги `_changed`. | 1. Внести изменения (создать процесс, обновить образец). 2. Дождаться завершения цикла. 3. Проверить БД: изменения сохранены. 4. Следующий цикл: без изменений — upsert не вызывается (или с нулевым количеством). | | `_save_objects_to_db` |